package com.epam.training.ticketservice;

import org.springframework.context.annotation.Configuration;

// Dont need to add any componentscans because things are added as dependencies in the pom and everything is in this top level package
@Configuration
public class Config {
}